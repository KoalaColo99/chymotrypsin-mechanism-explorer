# Assets

The current interactive is self-contained and uses inline SVG. Add any optional locally bundled, licensed structure reference files here; do not add runtime requests to remote PDB services.
