# Image assets

Reserved for repository-bundled classroom illustrations and thumbnails. The
current active-site fallback is vector SVG embedded in the application so it
remains sharp at projection scale and works when the page is opened locally.
